8800.life {
    root * /srv
    file_server

    # WebSocket чат — проксируем на бота
    reverse_proxy /ws/* bot:8888

    # Health check
    reverse_proxy /health bot:8888

    # SPA — если файл не найден, отдаём app.html
    try_files {path} /app.html
}
